import React from 'react';

interface PhaseMetricsProps {
  phase?: string;
  phaseSlug?: string;
  fallbackMetrics?: any[];
}

// Placeholder component - phase metrics functionality removed
const PhaseMetrics: React.FC<PhaseMetricsProps> = ({ phase, phaseSlug, fallbackMetrics }) => {
  return null;
};

export default PhaseMetrics;