/**
 * AlphaFeedbackButton - DEPRECATED (Sprint 12)
 *
 * The floating feedback button has been removed in Sprint 12.
 * Feedback is now accessible via the Feedback Hub icon in the header.
 * This component returns null to prevent rendering if imported elsewhere.
 */
export function AlphaFeedbackButton() {
  return null;
}
