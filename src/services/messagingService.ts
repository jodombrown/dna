/**
 * @deprecated Use messageService from '@/services/messageService' instead
 * 
 * This file is kept for backward compatibility but will be removed in future versions.
 * Please migrate to the new messageService which provides better error handling,
 * type safety, and additional features.
 */

import { messageService } from './messageService';

export const messagingService = messageService;
